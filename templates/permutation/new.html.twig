{% extends "base.html.twig" %}

{% block body %}

	<div class="container col-md-6 col-offset-3" style="margin-top:100px">
		{{ form_start(form) }}
		<div class="col-md-6">{{ form_row(form.destination, {
			placeholder: 'localite desirée', 'attr': {'class': 'form-control'}
		}) }}</div>

		<input type="submit" value="Créer" class="btn btn-primary mt-3 col-md-6">
		{{ form_end(form) }}
	</div>


{% endblock %}
